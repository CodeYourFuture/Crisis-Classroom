import React, { Component } from "react";
import Form from "./form";

export default class Login extends Component {
  render() {
    return (
      <div className="App">
        <Form/>
      </div>
    );
  }
}